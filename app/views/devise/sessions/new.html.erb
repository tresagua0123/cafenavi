
<%= simple_form_for(resource,
 as: resource_name,
 url: session_path(resource_name), html: {class: 'form-signin mt-5' }) do |f| %>
 <h2 class="text-center">Log in</h2>
    <%= f.input :email,
                required: false,
                autofocus: true,
                label: false,
                placeholder: "メールアドレス" %>
    <%= f.input :password,
                required: false,
                label: false,
                placeholder: 'パスワード' %>
    <%= f.button :submit, "Log in", class: 'btn btn-primary btn-block' %>
<% end %>



<%= render "devise/shared/links" %>
